import { AuthLibRefreshTokensRepository } from './auth-lib-refresh-tokens.repository';
import { AuthLibUsersRepository } from './auth-lib-users.repository';

export const Repositories = [AuthLibRefreshTokensRepository, AuthLibUsersRepository];
